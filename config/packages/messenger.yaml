framework:
    messenger:
        transports:
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                options:
                    queue_name: metier_attractivite
            sync: 'sync://'

        routing:
            App\Message\ImportMetierAttractiviteMessage: async
            App\Message\GenerateRecommendationMessage: async

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
